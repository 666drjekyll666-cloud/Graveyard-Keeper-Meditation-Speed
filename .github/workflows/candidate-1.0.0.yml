name: 1.0.0 Candidate Build

on:
  push:
    branches:
      - dev/1.0.0
    paths:
      - MeditationSpeed.csproj
      - src/**
      - .github/workflows/candidate-1.0.0.yml

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore MeditationSpeed.csproj

      - name: Build Release
        run: dotnet build MeditationSpeed.csproj -c Release --no-restore

      - name: Hash artifact
        run: sha256sum bin/Release/MeditationSpeed.dll | tee MeditationSpeed-1.0.0.sha256

      - name: Upload candidate
        uses: actions/upload-artifact@v4
        with:
          name: MeditationSpeed-1.0.0
          path: |
            bin/Release/MeditationSpeed.dll
            MeditationSpeed-1.0.0.sha256
          if-no-files-found: error
          retention-days: 30
